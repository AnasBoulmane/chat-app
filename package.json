{
  "name": "ssextention",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1",
    "npack-scripts": "3.4.10",
    "supertest": "^4.0.2"
  },
  "scripts": {
    "start": "npack-scripts start",
    "build": "npack-scripts build",
    "test": "npack-scripts test",
    "eject": "npack-scripts eject"
  },
  "eslintConfig": {
    "extends": "npack-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "entries": {
    "index": "./src/index"
  }
}
